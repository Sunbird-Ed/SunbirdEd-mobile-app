export enum AggregatorPageType {
    COURSE = 'course',
    LIBRARY = 'library_v2',
    HOME = 'home',
    SEARCH = 'search'
}

export enum Orientation {
    HORIZONTAL = 'horizontal',
    VERTICAL = 'vertical'
}
